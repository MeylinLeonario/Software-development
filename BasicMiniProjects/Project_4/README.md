# **Validador de contraseñas** — Evalúa contraseñas según diferentes reglas y devuelve exactamente qué condiciones no cumplen.

### Observaciones
En este proyecto desarrollé capas: interface e implements, donde todo se desarrolla en `Program.cs`.
Para implementarla en `Program.cs`, debemos de usar:

NombreClase + nombreVariable = new NombreClase();
Ejemplo:
`Implements validator = new Implements()`