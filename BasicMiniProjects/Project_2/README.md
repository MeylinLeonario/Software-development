# Conversor de unidades — Convierte temperatura, distancia, peso y tiempo. Evita llenar el programa de if gigantes.

Para este proyecto, requerimos de no llenar el programa de if's gigantes.

### Observaciones
- Siempre agregar el .csproj para que pueda correr el proyecto a través de `dotnet new console` y corerlo con `dotnet build`